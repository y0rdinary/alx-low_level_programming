#include <stdio.h>
/**
 * main - Entry point
 *
 * printf: prints a string
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char My_message[] = "with proper grammar, but the outcome is a piece of art,";

printf("%s\n", My_message);
return (0);
}
